import 'package:get/get_navigation/src/routes/transitions_type.dart';

class AppTransitions {
  static const duration = Duration(milliseconds: 400);
  static Transition defaultTransition = Transition.rightToLeft;
}